# Navigation

F-16C主要通过惯性导航系统（INS，Inertial Navigation System）进行导航，并由全球定位系统（GPS，Global Positioning System）接收机、中央航空数据计算机（CADC，Central Air Data Computer）以及模块化任务计算机（MMC，Modular Mission Compute）内的转向点（Steerpoints）导航数据库提供辅助。飞机通电后（无论是来自发动机驱动的发电机还是外部地面电源），飞行员必须启动INS的对准（Alignment）。一旦完成对准，INS将在三维“系统导航解算”中持续计算飞机的航向、姿态、高度和速度，从而提供水平导航位置以及垂直高度位置。

在飞行过程中，F-16C持续接收来自GPS卫星的位置更新，以提高INS系统导航解算的可靠性和精度。如果GPS信号不可靠或不可用，INS系统导航解算可能会随着时间的推移逐渐积累微小的误差。在这种情况下，可以通过执行“位置修正（Position Fix）”或“高度校准（Altitude Calibration）”在飞行中对INS进行更新。

F-16C还利用几个外部传感器向飞行控制计算机（FLCC，Flight Control Computer）和中央航空数据计算机（CADC）提供数据。

1. 机鼻安装的皮托管（空速管）向CADC和FLCC提供静压和总压数据，并驱动仪表板上的模拟高度表和空速/马赫数表；
2. 机身侧面安装的大气数据探针同样向CADC和FLCC提供静压和总压数据，以及攻角（AOA）和侧滑数据；
3. 机鼻两侧的一对攻角（AOA）传感器向 FLCC 提供成比例的攻角数据；
4. 机鼻两侧的一对静压孔向FLCC提供差值侧滑数据；
5. 右侧前缘展弦（边条翼）下方的一个总温探针向CADC提供模拟温度数据，用于计算真实空速（TAS）和大气的密度。

在低能见度条件下作战时，F-16C装备了以下设备：通过MIDS LVT终端实现的战术空中导航（TACAN，Tactical Air Navigation）能力、仪表着陆系统（ILS）和指点信标无线电接收机，以及用于精确测量飞机离地高度（AGL）的组合高度雷达高度表（CARA）。

## 一、惯性导航

惯性导航系统是F-16C的主要导航系统。一旦完成对准，INS就会利用内部的加速度计和陀螺仪传感器，在三维空间中追踪飞机的方向（姿态）和位置，从而为导航提供飞机的航向、姿态、高度、速度以及水平和垂直引导。INS还接收来自全球定位系统（GPS）卫星的数据，以提高可靠性和精度。

飞机的方向、位置和速度的组合值作为“系统导航解算（System Navigation Solution）”由INS输出，并被各种机载系统所利用。除了导航引导外，系统导航解算还决定了机载传感器的方向和位置精度、通过数据链传输的信息、武器弹道计算，以及传输给精密制导武器（智能弹药）的目标数据。

在机载模块化任务计算机（MMC）中，分别独立维护着三种导航解算方案：

- 仅INS方案（INS-only）：仅利用INS内部陀螺仪和加速度计的惯性数据。
- 仅GPS方案（GPS-only）：利用从GPS卫星接收到的位置和时间信号。
- 混合方案（Blended）：利用MMC内的卡尔曼滤波器（Kalman filter），将来自INS和GPS的数据进行结合。

### 1.1 INS Alignment

可以在“航电电源（AVIONICS POWER）”控制面板上选择并执行三种类型的INS对准。如果执行“常规陀螺罗盘对准”或“存储航向对准”，绝对不能以任何方式移动飞机或调整飞机位置，这包括重新配置外挂设备或对任何武器挂架进行挂弹/重新挂弹。

1. 常规陀螺罗盘对准（NORM）：在大约**8分钟**内完成INS对准，达到完全的位置信任度。
2. 存储航向对准（STOR HDG，Stored Heading Alignment）：在大约**90秒**内快速对准INS，但这要求飞机在经历上一次“常规陀螺罗盘对准”后关机期间，没有被移动过。
3. 空中对准（IFA）：允许在飞行过程中进行INS对准，但要求在对准过程中保持平飞姿态、恒定速度和恒定航向。需要GPS数据才能达到可靠的对准质量。

**注意**：当 DCS 任务开始且玩家必须自己启动F-16（冷舱启动）时，地勤人员总是会提前对飞机进行预对准，从而允许玩家在需要时执行STOR HDG。

在启动对准之前，必须满足以下条件：

1. 交流电母线（AC Bus）已通电，电力可来自发动机驱动的主发电机/备用发电机，或外部地面电源。
2. 模块化任务计算机已通过航电电源控制面板上的MMC开关启用。电子水平情势指示器（EHSI）将通电，但会显示INU警告旗，表明INS尚未提供数据。
3. 前上控制板（UFC，包含 ICP 和 DED）已通过航电电源控制面板上的UFC开关启用。数据输入显示器（DED）将初始化到CNI（通信/导航/识别）页面。

如果执行常规陀螺罗盘对准（NORM），必须将仪表板上的高度表设置为主运行模式——方法是短暂将模式开关保持在ELEC（电子）位置，直到 PNEU（气动）警告旗消失，并通过调节气压设定旋钮，将高度表本身校准为当前机场的海拔高度。如果执行存储航向对准，则不需要此步骤。

#### .1 INS初始化

“航电电源（AVIONICS POWER）”控制面板上的INS旋钮用于控制INS的模式，以及为INS进行初始化。其挡位如下：

1. `OFF`，关闭INS的电源。如果要将旋钮从`OFF`移动到其他挡位、或是关车，至少保证在旋入`OFF`挡后至少10秒再执行前两者操作；
2. `STOR HDG`，读取之前MMC中存放的校准的数据；
3. `NORM`，根据手动输入的数据校准INS；
4. `NAV`，在完成校准之后，将INS设置为一般导航模式；
5. `IN FLT ALIGN`，利用GPS数据或手动定位来启动对准，以获取位置数据；
6. `ATT`，仅提供备用俯仰角、横滚角和航向信息，当INS对准不再适用于导航但姿态数据仍然有效时使用。除TACAN外，其他导航数据将不可用。

在执行INS对准时，INS DED页面允许飞行员监控对准状态，并且与ICP结合使用时，允许飞行员根据正在执行的对准类型输入位置、高度和航向数据（如果需要）。

#### .2 INS DED Page

在ICP面板通过`List` $\rightarrow$ `6 INS`即可访问`INS DED`页面。每当INS旋钮被旋至`STOR HDG`或者`NORM`时，DED页面将自动跳转到`INS DED`。

<!-- 图片 -->

1. `Alignment Time/Status`，斜线左侧表示已经对齐的时间，单位为分钟，例如`1.6`表示大概1分36秒；斜线右侧表示对齐的状态，随着时间递减：
    - `99`，尚未进行INS校准。
    - `98-91`，INS惯性平台正在被调用，飞机正在获得真实的航向。
    - `90`，INS已经获得姿态数据，开始粗校准。一般在开始校准的30秒后达到此阶段。
    - `79-63`，INS正在粗校准。
    - `62`，尽管比完全校准的精度低，但是INS已经达到了可用的粗校准状态。
    - `60-11`，INS正在进行精细对准，这由圆误差概率（CEP，Circular Error Probability）乘数指示。CEP乘数为10对应于完全对准状态的精度为1.0，而CEP乘数为60对应于精度误差为完全对准状态的6.0倍。随着对准状态的降低，CEP值减小，表明INS对准精度提高。
    - `10`，INS已经完全校准，并且达到了CEP为1.0的状态。
2. `RDY Status Message`，如果`NORM`或者`STOR HDG`对齐已经完成，`RDY`将会显示在`Alignment Time/Status`右侧，以表示INS已经达到了导航所需的最低对齐要求。
3. `Latitude`，以`DD.MM.M`的格式显示纬度，在进行`NORM`校准时可以手动输入。
4. `Longitude`，以`DDD.MM.M`的格式显示精度，在进行`NORM`校准时可以手动输入。
5. `System Altitude`，显示飞行器的高度，单位为海平面以上多少英尺（MSL， mean sea level），在进行`NORM`校准时可以手动输入。
    - 注意，在执行INS的`NORM`对齐时，需要把高度计从气压切换到电子`ELEC`模式。
6. `True Heading`，显示INS输出的航向。然后，根据磁偏角数据`MAGV DED`页面上的磁偏角设置，推导出飞机的磁航向。
7. `Ground Speed`，显示INS输出的地速，单位为`knots/h`。
8. `Selected Steerpoint`，显示选择的转向点。

#### .3 INS INFLT ALGN DED Page

在ICP面板通过`List` $\rightarrow$ `6 TIME` $\rightarrow$ `SEQ`，可以将DED切换到到`INS IFA`页面。该页面允许飞行员在没有GPS的情况下手动输入磁航向来进行IFA校准。

在进行GPS辅助IFA对准时，飞机的位置和高度数据完全来自GPS数据。GPS接收器必须通电，并且与天空保持畅通无阻的视线，才能从多个GPS卫星获取数据（即，飞机不能位于机库或加固的飞机掩体内）。如果GPS数据不可用，则会显示“需要定位`FIX NECESSARY`”消息，表明必须执行位置定位才能完成对准。

<!-- 图片 -->

1. `Compass Heading`，显示飞机的磁航向，可以根据`MAGV DED`页面的磁场变化显示为真实航向。
2. `Selected Steerpoint`，显示选择的转向点。

#### .4 MAGV DED Page

当DED显示`MISC`页面时，在ICP键盘上按下`2/ALOW`键，即可进入“磁偏角”DED页面。该页面显示真航向（True Heading）与磁航向（Magnetic Heading）之间的磁力差值，该差值在全球范围内因地理位置不同而异。磁偏角设定可以根据飞机内存储的数据表自动确定，也可以使用ICP手动输入。

1. 磁偏角模式：显示用于将INS真航向数据转换为磁航向的磁偏角数据来源。可通过将ICP上的DCS开关短暂推至`SEQ`位置，在`AUTO`和`MAN`之间进行切换。
    - `AUTO`：根据存储在飞机内存中的数据表进行转换。飞行员无法编辑磁偏角，但飞机会根据其所处的地理位置自动进行更新。
    - `MAN`：根据在此页面上手动输入的磁偏角值进行转换。飞行员可以编辑磁偏角，但它不会根据飞机的地理位置自动更新。
2. 磁偏角：显示用于将真航向转换为磁航向的磁偏角具体数值。如果磁偏角模式设置为`MAN`，可通过将DED的星号（*）移至该数据字段，使用ICP键盘输入磁偏角数值，然后按下`ENTR`键进行修改。
    - 按下键盘上的按钮`4`代表西（West），或按钮`6`代表东（East），随后紧跟精确到小数点后一位的偏角数值本身（如有必要，前方需补零），输入时为一串连续的数字，不包含小数点。
    - 例如：磁偏角“东偏 05.5 度（E05.5）”应输入为“6055”，然后按下`ENTR`。

#### .5 正常罗盘对齐 NORM

当执行常规陀螺罗盘对准时，必须在对准启动后的两分钟内，输入飞机当前的纬度、经度和高度。真航向是由系统自动测定的。

如果这些数据值已经是准确的（即它们已经正确反映了飞机当前的纬度、经度和/或高度），仍必须通过在数据字段周围放置DED星号（*）并在ICP上按下`ENTR`键来进行确认。如果启动对准后两分钟内未输入（或确认）纬度、经度和高度，对准将会失败并自动重新开始。

要完成INS的常规陀螺罗盘对准，请执行以下操作：

1. `INS`旋钮 – 拨至`NORM`位置。DED屏幕将自动跳转并显示INS页面。
2. 调整纬度：
    - `ICP`键盘 – 输入`2`代表北（North）或`8`代表南（South）。
    - `ICP`键盘 – 以“度度°分分.分'（DD°MM.M’）”的格式输入纬度，在输入时表现为一串连续的五位数字（DDMMM）。
    - `ICP`键盘 – 按`ENTR`键接受数据，或按`RCL`（清除）键拒绝数据并重新输入。
3. 调整经度：
    - `ICP DCS`开关 – 向下拨动，将 DED 的星号移至经度（LNG）数据字段周围。
    - `ICP`键盘 – 输入`6`代表东（East）或`4`代表西（West）。
    - `ICP`键盘 – 以“度度度°分分.分'（DDD°MM.M’）”的格式输入经度，在输入时表现为一串连续的六位数字（DDDMMM），如有必要，前方需补零。
    - `ICP`键盘 – 按`ENTR`键接受数据，或按`RCL`键拒绝并重新输入。
4. 调整高度：
    - `ICP DCS`开关 – 向下拨动，将DED的星号移至系统高度（SALT）数据字段周围。
    - `ICP`键盘 – 输入以英尺（feet）为单位的高度值。
    - `ICP`键盘 – 按`ENTR`键接受数据，或按`RCL`键拒绝并重新输入。
5. 等待完全对齐后，将`INS`旋钮 – 拨至`NAV`位置。

无论DED是否一直显示INS页面，对准都会在后台继续进行。当INS对准状态码递减到`62`时（通常在 2 分钟或更短时间内），`INS DED`页面上将显示`RDY`，同时HUD的左下角将显示`ALIGN`字样，取代原本显示最大G值的物理位置。

**注意**：上述经纬度、高度也可以通过GPS自动获取，或者在`INS DED`页面通过航路点指定。

**自动保护机制 AUTO-NAV**

如果系统已经达到了“降级对准”（状态码降至`62`以下），此时如果INS检测到飞机的任何移动（无论是来自飞机开始滑行，还是地勤人员正在重新配置外挂武器），即使INS旋钮仍处于`NORM`位置，INS也会在内部自动进入`NAV`模式。

当进入`AUTO-NAV`模式时，会发生以下情况：

- `RDY`字样从`INS DED`页面上消失。
- `ALIGN`字样从HUD上消失，重新替换为最大G值。
- 对准计时（Time Into Alignment）停止递增。
- 对准状态码（Alignment Status）停止递减。

**注意**：即使INS已经自动进入了`AUTO-NAV`模式，在起飞前仍必须手动将INS旋钮设置到`NAV`位置。

**自动保护机制 NARF**

如果INS进入`AUTO-NAV`模式后，INS旋钮仍设置在NORM位置，且INS不再检测到飞机运动，则INS将自动初始化一种名为“导航自动重新对准功能（NARF）”的伪对准模式。当NARF激活时，只要飞机保持静止，惯导单元（INU）就会继续精细化和提升对准质量。

当NARF激活时，会发生以下情况：

- 对准计时将恢复递增。
- 对准状态码将恢复递减。

如果已经达到了降级对准（由`INS DED`页面上的`RDY`和HUD中的`ALIGN`指示），可以通过将INS旋钮旋转到`NAV`位置来手动中断对准。这将允许飞机进行滑行和/或允许地勤人员重新配置外挂武器。

如果INS先进入了`AUTO-NAV`模式，随后又进入了`NARF`模式，此时将INS旋钮短暂旋转到`NAV`位置然后立刻拨回`NORM`，将会关闭NARF模式，并全面恢复标准的常规陀螺罗盘对准。

当INS对准状态码达到`10`时（通常在 8 分钟内），`INS DED`页面上的`RDY`将开始闪烁，HUD中的`ALIGN`也开始闪烁。这表明对准已完全结束，达到最高精度。

#### .6 快速（存储航向）对齐 STOR HDG

快速对齐的逻辑与NORM基本相同，但是操作简化为：

1. 先获取GPS信号；
2. 将INS旋钮打到`STOR HDG`；
3. 等待对齐后将旋钮打到`NAV`。

在对齐过程中，同样会进入`AUTO-NAV`模式，

#### .7 IFA对齐（有GPS）

当借助GPS数据执行空中对准时（无论是在飞行中还是在地面静止），GPS接收机必须已通电，并且与天空之间拥有无遮挡的视线，以便从多颗GPS卫星获取数据（即飞机不能位于机库或加固飞机掩体/机堡内）。

**注意**：在空中对准的初始阶段，INS惯性平台正在进行水平校准并获取真航向，此时飞机必须保持平飞、无加速度的飞行状态，或者在地面保持静止，以最大限度地减少INS的精度衰减。

要完成带GPS辅助的空中对准，请执行以下操作：

1. 利用备用仪表建立平飞，保持恒定的航向、高度和空速；或者在地面保持静止。
2. `INS`旋钮，拨至`IN FLT ALIGN`位置。
    - DED屏幕将自动跳转显示`INS INFLT ALGN`页面，同时HUD左下角原本显示最大G值的物理位置将显示`STBY`。
3. （可选）`ICP DCS`开关，推至`SEQ`位置，以显示标准的`INS DED`页面并监控对准状态码。
4. 校准后，`INS`旋钮，拨至`NAV`位置。

#### .8 IFA对齐（无GPS）

可以在没有GPS数据辅助的情况下执行空中对准。然而，这样对准的质量将会严重降低。此外，飞机的飞行路径附近必须有一个可识别的地面地标，并且该地标已作为转向点（航点）保存到导航数据库中，以便对其执行“位置修正（Position Fix）”。

**注意**：在空中对准的初始阶段，INS惯性平台正在进行水平校准并获取真航向，此时飞机必须保持平飞、无加速度的飞行状态，或者在地面保持静止，以最大限度地减少INS的精度衰减。

要完成无GPS辅助的空中对准，请执行以下操作：

1. 利用备用仪表建立平飞，保持恒定的航向、高度和空速；或者在地面保持静止。
2. `INS`旋钮，拨至`IN FLT ALIGN`位置。
    - DED屏幕将自动跳转显示`INS INFLT ALGN`页面，同时HUD左下角原本显示最大G值的物理位置将显示`STBY`。
3. ICP键盘，输入飞机当前的磁航向，不需要输入前导零（例如：045° 只需输入“45”）。
4. ICP键盘，按`ENTR`键接受数据，或按`RCL`键拒绝并重新输入。当INS获取到真航向时（通常在20秒内），HUD左下角的`STBY`将被替换为`MAN`。
5. ICP DCS开关，推至`SEQ`位置，以显示标准的INS页面并监控对准状态码。
6. 当对准状态码降至`75`时，将ICP DCS开关推至`RTN`位置。
7. ICP键盘,按下`8/FIX`键。使用火控雷达（FCR）或目标指示吊舱（TGP）执行一次“位置修正（Position Fix）”，以获取当前的位置和速度数据。
8. INS旋钮，拨至`NAV`位置。

#### .9 姿态模式 (ATT)

在姿态模式下运行时，INS将仅提供俯仰（Pitch）、滚转（Roll）和航向（Heading）信息，而不会提供导航数据。这要求飞行员必须完全依赖TACAN（战术空中导航）进行导航，甚至使用时间/距离/航向进行航位推算（Dead Reckoning）。

如果在需要立即起飞的情况下，不允许INS达到粗对准（Coarse Alignment），或者如果INS的位置和/或速度精度已退化到无法再用于导航的程度，但姿态和航向数据仍然有效，则可以使用此模式。

当INS设置为姿态模式时，电子水平情势指示器（EHSI）将显示一个黄色的ATT警告旗，但会继续提供磁航向指示。然而，EHSI将仅显示由TACAN或ILS无线电信号提供的导航数据；EHSI不会提供到任何转向点（航点）的方位、距离或航道偏离信息。如果怀疑EHSI磁航向罗盘对准不准或不精确，可以通过按下HDG（航向）旋钮，然后根据需要旋转该旋钮来手动调整罗盘，以纠正任何对准偏差。

虽然姿态模式不需要INS达到粗对准，但它确实需要INS获取有效的航向和姿态数据。在执行地面对准时，这通常在对准初始化后的30秒内达到，此时`INS DED`页面上的对准状态码显示为`90`。如果必须在达到粗对准之前立即起飞，可以在获取到有效的航向和姿态数据且对准状态码递减至`90`后，将INS设置为姿态（ATT）模式。

但此时HUD上的飞行路径标记（`FPM`）将无法正常工作，并将保持冻结在机轴十字（Boresight Cross）位置。此外，无论综合控制面板（ICP）上是否启用了“漂移切断（Drift C/O）”，俯仰刻度线（地平线/俯仰梯）都不会提供侧滑或风偏的指示。可以将HUD控制面板上的Scales（刻度）开关设置为`VV/VAH`位置，以在HUD中显示垂直速度刻度，从而提供爬升或下降的指示，用以替代失效的FPM。

因此，使用`ATT`模式：

1. 至少在INS校准到`90`之后再起飞；
2. `FPM`将失效；
3. HUD面板的`VV/VAH`可以显示一个垂直速度。

### 1.2 导航解决方案

在引入GPS等基于卫星的导航系统之前，军用飞机依赖于惯性导航系统（INS）来跟踪和维持它们在全球任何地方的预估位置。基于对姿态、速度和加速度的惯性测量，飞机的坐标位置将连续且自动地更新，而无需依赖TACAN（战术空中导航）等基于无线电的导航辅助设备。

然而，即使是使用最先进、最可靠的INS，加速度计内部的电子噪声以及对准时的不完美也会导致误差逐渐累积，从而导致飞机的预估位置与其实际物理位置之间产生“漂移（Drift）”。随着任务的推进，由于飞机的预估位置（被称为“导航解算值”）不断偏离飞机在任何给定时间的真实所在位置，这种漂移可能会导致导航和武器瞄准（Targeting）上产生严重的误差。

通过利用从环绕地球轨道的全球定位系统（GPS）卫星接收到的位置信号，可以通过应用来自GPS的修正量，在整个任务期间维持导航精度。F-16C的模块化任务计算机（MMC）利用卡尔曼滤波器算法，在数学层面上将INS和GPS数据结合成一个“混合（Blended）”导航解算方案，以同时获得INS的稳定性和自主性，以及GPS的高精度。

然而，只有当“纯INS”解算值与“纯GPS”解算值之间的差值（即Delta，$\delta$）超过300英尺（91米）时，才会应用这种修正。如果差值小于300英尺，MMC将不会应用任何修正，系统导航解算将继续完全基于“纯 INS”的计算结果。

导航系统的预估精度可以在DED的`NAV STATUS`（导航状态）页面上查看，并且可以在DED的`NAV COMMANDS`（导航指令）页面上彻底关闭MMC的卡尔曼滤波器，从而使用纯INS导航解算。

#### .1 NAV STATUS DED Page

在ICP面板通过`List` $\rightarrow$ `4 Nav`即可进入`NAV STATUS DED`页面。

<!-- 图片 -->

1. `System Accuracy`，显示由MMC卡尔曼滤波器评估的系统导航解算的精度，即使在`NAV COMMANDS DED`页面上将滤波器模式设置为`INS ONLY`（仅惯导），该精度仍会显示。如果混合导航解算不可用或GPS接收机已关闭，则不会显示该精度。
    - `HIGH`，评估的系统导航误差，小于300英尺（约91米）。要达到这种精度级别，必须使用带GPS辅助的混合解算方案。
    - `MED`，评估的系统导航误差，大于300英尺但小于6,000英尺（约1.8公里）。
    - `LOW`，评估的系统导航误差，大于6,000英尺（超过1.8公里）。
2. `GPS Accuracy`，显示GPS解算精度的预估值。如果GPS接收机未完全初始化或已关闭，则不会显示该状态。
    - `HIGH`，机载GPS接收机，正成功追踪至少4颗GPS卫星。GPS解算的估计水平位置误差小于300英尺。
    - `MED`，机载GPS接收机，追踪的卫星少于4颗，或者GPS解算的估计水平位置误差大于300英尺。
    - `LOW`，机载GPS接收机，没有追踪到任何GPS卫星。

**注意**：只有当DCS任务设定的日期在1994年3月28日或之后，GPS才可用。此外，除非美国（USA）是DCS任务中分配给玩家所在联盟（阵营）的国家之一，否则GPS的精度将会下降（降级）。

然而，如果以下两个条件同时满足，这些限制将被覆盖（失效）：

- 玩家的“游戏设置（GAMEPLAY）”选项中启用了“无限制卫星导航（Unrestricted SATNAV）”，或者当前游玩的任务选项中强制启用了该功能。
- 并且当前游玩的任务选项中没有强制禁用“无限制卫星导航”。

3. `Mission Duration`，显示需要有效GPS密钥的连续天数。暂未实现。
4. `GPS Key Status`，显示加载在GPS接收机内的加密密钥的有效性状态。

#### .2 NAV COMMANDS DED Page

<!-- 图片 -->

在ICP面板通过`List` $\rightarrow$ `4 Nav` $\rightarrow$ `SEQ`即可进入`NAV COMMANDS DED`页面。

1. `Filter Mode`，显示用于推导系统导航解算的滤波器模式。当DED星号（*）位于该数据字段上时，可以通过按下ICP键盘上的任意数字键`1-9`在`AUTO`和`INS ONLY`之间进行切换。
    - `AUTO`，当GPS数据可用时，MMC将利用卡尔曼滤波器结合GPS和INS的数据，以生成混合导航解算方案。GPS接收机必须已通电，并且与天空之间拥有无遮挡的视线，以便从多颗GPS卫星获取数据（即飞机不能位于机库或加固飞机掩体/机堡内）。
    - `INS ONLY`，系统导航解算将仅使用INS数据，即使此时GPS数据完全可用。
2. `Reset GPS`，重置（软重启）GPS接收机，这将重新启动卫星的搜星、捕获与追踪流程。可以通过将DED星号移至该数据字段两侧，并按下ICP键盘上的`0/M-SEL`（功能选择）键来下达指令，随后该数据字段内的文字将变亮高显，以确认指令已执行。
3. `Zeroize GPS`，擦除加载到GPS接收机中的高精度加密密钥。擦除后GPS辅助依然可用，但GPS数据的精度将会显著降低（降级为民用精度）。可以通过将DED星号移至该数据字段两侧，并按下`0/M-SEL`键来下达指令，文字高显代表确认执行。

#### .3 系统高度 (SALT)

作为一款具备对地打击能力的通用多用途战斗机，F-16的模块化任务计算机（MMC）在进行武器投掷计算时，其核心关键在于准确确定飞机在平均海平面（MSL）之上的高度。通过结合外部大气压力、温度和惯性加速度的值，并在GPS信号和数字地形数据的辅助下，MMC生成了一个“系统高度解算方案”。该方案用于在抬头显示器（HUD）和头盔显示系统（HMCS）中准确显示符号、提供更精确的高度和垂直速度指示，并确定传感器和瞄准光标的合适高低角（俯仰角）。

MMC系统高度源自以下三种渠道之一，其优先级高低到低：

1. **混合**高度：结合了纯惯导高度以及从GPS卫星或数字地形系统（DTS）接收到的垂直位置数据。
2. **惯导**（INS-only）高度：利用惯导系统内部加速度计的惯性数据，并结合由CADC导出的高度数据。
3. **CADC**高度：利用来自外部空气传感器的原始气压数据。

如果混合高度无效或未启用，MMC将使用“纯惯导高度”作为系统高度。如果混合高度和纯惯导高度均无效或未启用，MMC则将使用“CADC高度”作为系统高度。

中央大气数据计算机（CADC）提供大气压力和温度数据，用于计算真实空速和空气密度，然后将这些数据与源自惯性导航系统（INS）的垂直加速度数据相结合。引入惯性加速度所生成的系统高度，对垂直速度和飞机运动的变化更具响应性，从而克服了传统模拟系统中（纯粹依赖探测飞机周围外部静压变化）通常存在的延迟。

如果对INS进行常规陀螺罗盘对准（Normal Gyrocompass Alignment，即地面冷启动对准），纯惯导高度将基于CADC高度进行初始化。因此，在启动INS对准之前，飞行员务必确保仪表板上的高度计已设置为ELEC（电子）工作模式——具体操作为：将模式开关保持在ELEC位置，直到PNEU（气动）警告旗消失，并通过调节气压设定旋钮将高度计校准为当前机场的海拔高度。这确保了纯惯导高度能够准确地初始化为飞机在平均海平面（MSL）之上的实际高度。

然而，正如纯惯导导航解算会随时间推移积累误差一样，纯惯导高度也可能会产生“漂移”，导致飞机的预估高度与真实高度之间出现误差。系统高度的准确性可以通过使用GPS卫星或机载数字地形系统（DTS）的数据进行自动更新；或者在必要时，飞行员也可以通过执行“高度校准（Altitude Calibration）”来手动修正纯惯导高度中已经积累的误差。

通过将`ACAL DED`航页设置为`AUTO`子模式，即可启用自动高度校准。当`ACAL`设置为`AUTO`时，MMC将利用GPS或DTS数据生成一个“混合（Blended）”系统高度，以克服任务期间大气压力的波动以及纯惯导高度内部积累的误差。

与“混合导航解算”不同（混合导航解算是指当纯惯导和纯GPS解算之间的增量（Delta）超过300英尺时，MMC通过卡尔曼滤波器引入由GPS导出的修正值），MMC生成混合系统高度解算方案时，则是不经过卡尔曼滤波器，而是连续直接应用由GPS或DTS导出的修正值。然而，只有当GPS和/或DTS的垂直位置精度在所选主模式的指定阈值之内时，才会应用这些修正，具体要求如下所述。

在满足以下条件的前提下，`AUTO ACAL DED`航页允许飞行员选择MMC使用哪种数据源（GPS、DTS 或两者同时）来生成混合系统高度：

- `GPS`：机载GPS接收机的**垂直位置精度**必须在相应主模式的阈值之内：
    - `A-G`主模式：小于**50**英尺。
    - 所有其他主模式：小于**100**英尺。
- `BOTH`：机载 GPS接收机和DTS地形参考导航的**垂直位置精度**均必须在相应主模式的阈值之内：
    - `A-G`主模式：GPS小于**50**英尺；DTS小于**20**英尺。
    - 所有其他主模式：GPS小于**100**英尺；DTS小于**100**英尺。
- `DTS`：DTS 地形参考导航的**垂直位置精度**必须在相应主模式的阈值之内：
    - `A-G`主模式：小于**20**英尺。
    - 所有其他主模式：小于**100**英尺。

当`AUTO ACAL`设置为`BOTH`时，实际上只能使用一种数据源来生成混合系统高度。MMC将自动选择那个正在提供最精确数据的传感器，前提是该传感器的垂直位置精度在相应的阈值之内。

如果由于GPS和/或DTS数据超过了相应的阈值，或者它们变得无效/不可用，导致无法生成混合系统高度，系统高度将降级退回至“纯惯导高度”，直到有效的GPS和/或DTS 数据恢复。如果纯惯导高度也变得无效或不可用（无论是由于INS电源关闭、未对准，还是INS以某种方式发生故障或损坏），系统高度将降级退回至“CADC（大气数据计算机）高度”。

通过将`ACAL DED`航页设置为`MAN`子模式，可以关闭自动高度校准。当`ACAL`设置为MAN时，混合系统高度解算方案被禁用，MMC系统高度将退回到“纯惯导高度”。

##### .1 Digital Terrain System (DTS) 

数字地形系统（DTS）是F-16C机载航电系统的一个软件组件，它利用存储在飞机内存中的数字地形高程数据（DTED），独立于火控雷达（FCR）或GPS等其他飞机传感器，来提供精确的导航和高度解算方案。当通过数据传输卡带（DTC）将DTED数据加载到机载内存中时，DTS会创建任务区域的数字飞行地图（DFM），这是一个可用于导航和武器投掷功能的三维地形模型。

在导航方面，DTS可以利用雷达高度计，通过一项被称为地形参考导航（TRN）的功能，在数字飞行地图（DFM）上“追踪”飞机的绝对位置。在飞行过程中，通过测量飞机下方地形高度的变化，TRN程序会生成一段飞机下方的地形轮廓线（地形剖面），然后将该地形轮廓线与DFM的三维地形模型进行相关性比对（Correlates）。随后，将TRN预估的飞机位置和高度与惯导系统（INS）进行对比，以确定是否存在误差。TRN过程是全自动的，不需要飞行员干预；然而，DTS需要INS完成对准才能正常工作。

除了有效的INS数据外，TRN还需要飞机地速（GroundSpeed）保持在约150至640节（海里/小时）之间才能可靠工作。如果INS断电、未对准、或以某种方式发生故障或失效，亦或者地速超出了可用阈值，TRN将被禁用。TRN的状态和精度由DTS软件持续评估，该软件会在`AUTO ACAL DED`航页上分别显示TRN的预估水平精度和垂直精度。TRN的精度取决于以下因素：

1. 数字地图（DFM）的准确性和分辨率，这取决于存储在DTC（卡带）上的精确DTED数据。
2. 组合航空雷达高度计（CARA）的精度：其工作限制为地面之上**50,000**英尺以内；且在**3000**英尺以下时，飞机的俯仰/滚转姿态需在$\pm60^\circ$以内。在3000英尺以上时，姿态限制可能会更小。
3. 飞机正在飞越的地形特征。

如果飞机正飞越高山或起伏的丘陵，且地形拥有足够的垂直高差变化，TRN将生成一个能够精准对应到DFM上独特位置的地形轮廓线，从而缩小TRN的位置不确定性（即系统中的阴影区域），并提高任何由DTS导出的、用于混合系统高度的修正值的精度。

在目标瞄准和武器投掷方面，如果TRN正在DFM（数字地图）上成功追踪飞机的精确位置，DTS就可以被用作一个无源（被动）测距传感器，用以确定SPI的海拔高度。该功能完全独立于其他飞机传感器，如火控雷达的空对地测距（FCR AGR）或瞄准吊舱（TGP）的激光测距仪/指示器。

